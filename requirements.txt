# UI
gradio>=5.0.0
pandas>=2.0.0
plotly>=5.18.0

# OpenAI Agents SDK (import: agents)
openai-agents>=0.1.0
openai>=1.40.0

# MCP (stdio client + FastMCP servers under mcp.server.fastmcp)
mcp>=1.0.0

# Market data
polygon-api-client>=1.13.0

# Config & HTTP
python-dotenv>=1.0.0
requests>=2.31.0

# Models / validation (accounts, push_server)
pydantic>=2.0.0
